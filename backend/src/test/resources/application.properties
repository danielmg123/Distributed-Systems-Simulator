# Activates the 'dev' profile for all test runs, so tests get the same
# security-disabled / fallback-JWT-secret behavior they relied on before
# application.properties stopped shipping those as insecure defaults.
# Individual tests (e.g. SecurityConfigEnabledTest) still override specific
# properties via @TestPropertySource, which takes precedence over this.
spring.profiles.active=dev
