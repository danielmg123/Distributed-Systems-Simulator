# Test configuration. The application is unauthenticated (see SecurityConfig), so
# tests need no security/profile setup; they use the default datasource settings
# from the main application.properties (MongoDB on localhost:27017).
